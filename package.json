{
  "name": "perfectsand-render-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "postinstall": "npm --prefix backend install",
    "start": "npm --prefix backend start"
  }
}
